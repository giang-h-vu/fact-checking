apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ollama-data
  namespace: fact-checking
spec:
  accessModes: [ReadWriteOnce]
  storageClassName: managed-csi # Azure Managed Disk (default on AKS)
  resources:
    requests:
      storage: 10Gi
